[project]
name = "python-as-fast-as-possible"
version = "0.1.0"
description = "Interactive Python fundamentals tutorial with a browser-based Pyodide Codelab, notebook, and reference guide."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
notebook = [
  "jupyterlab>=4.0",
  "ipykernel>=6.0",
]
