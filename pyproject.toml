[project]
name = "python-as-fast-as-possible"
version = "0.1.0"
description = "Hands-on Python fundamentals tutorial based on the topic sequence of Tech With Tim's Python speed course."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
notebook = [
  "jupyterlab>=4.0",
  "ipykernel>=6.0",
]
